def solve(s):
    digits = ''
    letters = ''
    others = ''
    for ch in s:
        if ch.isdigit():
            digits += ch
        elif ch.isalpha():
            letters += ch
        else:
            others += ch

    print(digits)
    print(letters)
    print(others)


text = input()
solve(text)
